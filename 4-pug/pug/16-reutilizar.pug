doctype
html(lang="en")
	head
		meta(charset="UTF-8")
		title Reutilizable (Mixin)
	body
		h1 Reutilizable (mixin)
		mixin serie(name,caps)
			li= name+' ('+caps+')'

		+serie('inuyasha',60)
		+serie('prince of tennis',16)
		+serie('superman',2)

	